'use client'; // Make this a client component because of hooks and localStorage

import MCQQuiz from '../../components/MCQQuiz';

export default function QuizPage() {
  return <MCQQuiz />;
}
// This page is a client component that renders the MCQQuiz component